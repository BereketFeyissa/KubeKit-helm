apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "KubeKit.fullname" . }}-test-connection"
  labels:
    {{- include "KubeKit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "KubeKit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
